# Test key fixtures

Every key in this directory is a throwaway generated by `tools/gen_key_fixtures.sh` (real
`ssh-keygen` / `openssl`) purely so the parsers can be tested against real-world file formats.
They protect nothing and are not authorised anywhere. The passphrase of every `*_enc` key is
`correct horse`.
